{
  "name": "genlayer-card-generator",
  "version": "1.0.0",
  "private": true,
  "description": "GenLayer Community Card Generator",
  "scripts": {
    "local": "npx vercel dev --yes --listen 3000"
  },
  "dependencies": {
    "@upstash/redis": "^1.35.6"
  }
}
